{{-- Superseded by the full Tailwind build (tailwind.config.js scans every Blade
     file, so the prebuilt resources/dist/warden.css already contains every class
     used in markup). Kept as an empty include so the layout's <style> block and
     any published copies keep resolving. Regenerate the stylesheet with:
     npx tailwindcss@3 -c tailwind.config.js -i resources/css/warden.css -o resources/dist/warden.css --minify --}}
